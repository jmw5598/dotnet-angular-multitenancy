import { XyzCanUpdatePipe } from './can-update.pipe';

describe('XyzCanUpdatePipe', () => {
  it('create an instance', () => {
    const pipe = new XyzCanUpdatePipe();
    expect(pipe).toBeTruthy();
  });
});
