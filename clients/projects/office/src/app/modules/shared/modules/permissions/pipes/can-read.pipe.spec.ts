import { XyzCanReadPipe } from './can-read.pipe';

describe('XyzCanReadPipe', () => {
  it('create an instance', () => {
    const pipe = new XyzCanReadPipe();
    expect(pipe).toBeTruthy();
  });
});
