import { XyzCanCreatePipe } from './can-create.pipe';

describe('XyzCanCreatePipe', () => {
  it('create an instance', () => {
    const pipe = new XyzCanCreatePipe();
    expect(pipe).toBeTruthy();
  });
});
