import { XyzHasModulePermissionPipe } from './has-module-permission.pipe';

describe('XyzHasModulePermissionPipe', () => {
  it('create an instance', () => {
    const pipe = new XyzHasModulePermissionPipe();
    expect(pipe).toBeTruthy();
  });
});
