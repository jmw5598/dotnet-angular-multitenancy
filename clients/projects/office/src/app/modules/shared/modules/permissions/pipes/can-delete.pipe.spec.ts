import { XyzCanDeletePipe } from './can-delete.pipe';

describe('XyzCanDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new XyzCanDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
